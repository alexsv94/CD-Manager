<UserControl x:Class="OrganizerWpf.UserControls.DocumentsExplorer.NameCellTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrganizerWpf.UserControls.DocumentsExplorer"
             x:Name="rootCell"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="600
             ">
    <UserControl.Resources>
        <Style TargetType="Image" x:Key="Icon">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="1" />
        </Style>
    </UserControl.Resources>

    <Grid Height="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image Style="{StaticResource Icon}" Source="{Binding ElementName=rootCell, Path=Image}"/>       
        <TextBlock Grid.Column="1" Text="{Binding ElementName=rootCell, Path=DocName}" Margin="3,1,0,0"></TextBlock>
    </Grid>
</UserControl>
